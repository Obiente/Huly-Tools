---
// TableRow.astro: Reusable table row
// Props: cells (array of Astro components or strings)
const { cells = [] } = Astro.props;
---
<tr class="bg-white border-b">
  {cells.map(cell => cell)}
</tr>
